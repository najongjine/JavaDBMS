package com.biz.addr.SQL;

public class SQL {
	public static final String SELECT_ADDR=" select ID, " + 
			"NAME, " + 
			"TEL, " + 
			"ADDR, " + 
			"CHAIN from tbl_addr ";
	public static final String INSERT_ADDR=" insert into "
			+ "NAME, " + 
			"TEL, " + 
			"ADDR, " + 
			"CHAIN ";
}
